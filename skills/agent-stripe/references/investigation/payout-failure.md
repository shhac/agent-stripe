# payout-failure

Run `agent-stripe investigate payout-failure po_...` when a payout failed.

It retrieves the payout, related balance transaction, and failure fields such as code/message/destination.
