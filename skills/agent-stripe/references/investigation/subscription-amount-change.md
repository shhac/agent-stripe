# subscription-amount-change

Run `agent-stripe investigate subscription-amount-change --subscription sub_...` when an invoice amount changed or looks wrong.

It compares latest invoice, invoice lines, preview amount, subscription items, prices, and products.
