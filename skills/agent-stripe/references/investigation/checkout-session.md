# checkout-session

Run `agent-stripe investigate checkout-session cs_...` for Checkout completion questions.

It follows Checkout Session -> line items -> price/product metadata and resulting PaymentIntent, Charge, Subscription, Invoice, Customer, and Payment Link when present.
